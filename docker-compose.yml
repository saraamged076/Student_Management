version: '3.9'

services:

  fastapi:
    build: .
    container_name: student_api

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis_cache

    ports:
      - "6379:6379"